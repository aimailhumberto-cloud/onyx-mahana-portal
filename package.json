{
  "name": "mahana-portal",
  "version": "1.0.0",
  "scripts": {
    "build": "cd server && npm install && cd .. && npm install && npm run build",
    "start": "node server/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}